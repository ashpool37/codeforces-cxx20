{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug {{ problem_title }}",
            "program": "${workspaceFolder}/{{ problem }}.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}