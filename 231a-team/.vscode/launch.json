{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Team",
            "program": "${workspaceFolder}/231a-team.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}