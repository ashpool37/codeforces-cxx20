{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Chat room",
            "program": "${workspaceFolder}/58a-chat-room.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}