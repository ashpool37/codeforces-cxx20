{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Vanya and Fence",
            "program": "${workspaceFolder}/677a-vanya-and-fence.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}