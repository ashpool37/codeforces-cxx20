{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Watermelon",
            "program": "${workspaceFolder}/4a-watermelon.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}