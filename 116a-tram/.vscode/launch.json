{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Tram",
            "program": "${workspaceFolder}/116a-tram.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}