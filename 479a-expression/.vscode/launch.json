{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Expression",
            "program": "${workspaceFolder}/479a-expression.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}