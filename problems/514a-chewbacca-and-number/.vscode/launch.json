{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Chewbacca and Number",
            "program": "${workspaceFolder}/514a-chewbacca-and-number.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}