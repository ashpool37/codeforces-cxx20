{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Same Differences",
            "program": "${workspaceFolder}/1520d-same-differences.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
