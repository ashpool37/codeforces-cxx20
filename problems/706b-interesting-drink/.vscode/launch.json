{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Interesting drink",
            "program": "${workspaceFolder}/706b-interesting-drink.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}