{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Lucky?",
            "program": "${workspaceFolder}/1676a-lucky.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}