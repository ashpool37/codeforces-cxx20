{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Lecture",
            "program": "${workspaceFolder}/499b-lecture.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
