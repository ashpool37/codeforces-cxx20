{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug A+B Again?",
            "program": "${workspaceFolder}/1999a-a-b-again.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
