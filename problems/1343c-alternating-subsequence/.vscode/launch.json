{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Alternating Subsequence",
            "program": "${workspaceFolder}/1343c-alternating-subsequence.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
