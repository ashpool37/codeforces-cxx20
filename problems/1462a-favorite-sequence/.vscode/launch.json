{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Favorite Sequence",
            "program": "${workspaceFolder}/1462a-favorite-sequence.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
