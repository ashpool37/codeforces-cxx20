{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Panoramix's Prediction",
            "program": "${workspaceFolder}/80a-panoramix-s-prediction.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}