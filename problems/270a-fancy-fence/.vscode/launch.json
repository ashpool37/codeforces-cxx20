{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Fancy Fence",
            "program": "${workspaceFolder}/270a-fancy-fence.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
