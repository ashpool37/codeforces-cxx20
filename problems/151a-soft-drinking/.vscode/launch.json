{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Soft Drinking",
            "program": "${workspaceFolder}/151a-soft-drinking.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}