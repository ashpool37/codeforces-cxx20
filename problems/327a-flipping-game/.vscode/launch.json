{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Flipping Game",
            "program": "${workspaceFolder}/327a-flipping-game.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
