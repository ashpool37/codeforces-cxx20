{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Seating in a Bus",
            "program": "${workspaceFolder}/2000b-seating-in-a-bus.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
