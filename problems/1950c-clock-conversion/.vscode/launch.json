{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Clock Conversion",
            "program": "${workspaceFolder}/1950c-clock-conversion.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
