{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Spy Detected!",
            "program": "${workspaceFolder}/1512a-spy-detected.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}