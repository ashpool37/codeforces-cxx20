{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Legs",
            "program": "${workspaceFolder}/1996a-legs.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
