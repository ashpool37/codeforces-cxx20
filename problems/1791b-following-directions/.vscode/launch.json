{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Following Directions",
            "program": "${workspaceFolder}/1791b-following-directions.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
