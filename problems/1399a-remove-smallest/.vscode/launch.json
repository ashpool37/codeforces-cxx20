{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Remove Smallest",
            "program": "${workspaceFolder}/1399a-remove-smallest.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}