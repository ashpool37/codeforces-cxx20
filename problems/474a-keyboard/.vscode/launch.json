{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Keyboard",
            "program": "${workspaceFolder}/474a-keyboard.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
