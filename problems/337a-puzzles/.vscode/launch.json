{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Puzzles",
            "program": "${workspaceFolder}/337a-puzzles.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}