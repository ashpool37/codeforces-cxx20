{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Fox And Snake",
            "program": "${workspaceFolder}/510a-fox-and-snake.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}