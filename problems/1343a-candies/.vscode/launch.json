{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Candies",
            "program": "${workspaceFolder}/1343a-candies.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
