{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Required Remainder",
            "program": "${workspaceFolder}/1374a-required-remainder.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}