{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Balanced Array",
            "program": "${workspaceFolder}/1343b-balanced-array.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}