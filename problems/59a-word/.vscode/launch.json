{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Word",
            "program": "${workspaceFolder}/59a-word.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}