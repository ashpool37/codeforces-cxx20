{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Boredom",
            "program": "${workspaceFolder}/455a-boredom.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}