{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Maximum in Table",
            "program": "${workspaceFolder}/509a-maximum-in-table.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
