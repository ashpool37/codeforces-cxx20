{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Minimal Square",
            "program": "${workspaceFolder}/1360a-minimal-square.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
