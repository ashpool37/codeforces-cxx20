{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Queue at the School",
            "program": "${workspaceFolder}/266b-queue-at-the-school.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}