{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Division?",
            "program": "${workspaceFolder}/1669a-division.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}