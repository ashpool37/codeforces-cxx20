{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Game With Sticks",
            "program": "${workspaceFolder}/451a-game-with-sticks.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}