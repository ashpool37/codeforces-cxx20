{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Domino Piling",
            "program": "${workspaceFolder}/50a-domino-piling.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}