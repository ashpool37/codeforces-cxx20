{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Medium Number",
            "program": "${workspaceFolder}/1760a-medium-number.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}