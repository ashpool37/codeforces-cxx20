{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Card Game",
            "program": "${workspaceFolder}/1999b-card-game.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
