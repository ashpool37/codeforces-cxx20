{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Even Odds",
            "program": "${workspaceFolder}/318a-even-odds.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}