{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Array Divisibility",
            "program": "${workspaceFolder}/1983a-array-divisibility.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}