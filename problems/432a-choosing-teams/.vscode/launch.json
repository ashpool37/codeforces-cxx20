{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Choosing Teams",
            "program": "${workspaceFolder}/432a-choosing-teams.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}