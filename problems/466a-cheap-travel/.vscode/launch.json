{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Cheap Travel",
            "program": "${workspaceFolder}/466a-cheap-travel.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}