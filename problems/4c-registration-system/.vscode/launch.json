{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Registration system",
            "program": "${workspaceFolder}/4c-registration-system.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}