{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Maximum GCD",
            "program": "${workspaceFolder}/1370a-maximum-gcd.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
