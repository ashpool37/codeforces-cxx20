{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Borze",
            "program": "${workspaceFolder}/32b-borze.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}