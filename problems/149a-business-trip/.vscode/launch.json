{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Business trip",
            "program": "${workspaceFolder}/149a-business-trip.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
