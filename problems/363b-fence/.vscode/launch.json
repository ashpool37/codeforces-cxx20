{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Fence",
            "program": "${workspaceFolder}/363b-fence.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}