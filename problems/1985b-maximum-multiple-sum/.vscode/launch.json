{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Maximum Multiple Sum",
            "program": "${workspaceFolder}/1985b-maximum-multiple-sum.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
