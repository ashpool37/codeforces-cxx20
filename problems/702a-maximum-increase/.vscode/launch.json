{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Maximum Increase",
            "program": "${workspaceFolder}/702a-maximum-increase.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
