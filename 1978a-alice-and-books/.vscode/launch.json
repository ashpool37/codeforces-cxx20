{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Alice and Books",
            "program": "${workspaceFolder}/1978a-alice-and-books.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}