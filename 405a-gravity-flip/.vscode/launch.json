{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Gravity Flip",
            "program": "${workspaceFolder}/405a-gravity-flip.out",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}